<div class="modal fade" id="featuredEventModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
  <div class="modal-dialog">

    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal">&times;</button>
        <h4 class="modal-title">Add a featured event</h4>
      </div>
      <div class="modal-body text-justify">
        <div class="row">
          <div class="col-md-6 col-md-offset-3">
          <%= form_for :unscheduled_event, url: {:controller =>'unscheduled_events', :action => 'create'}, method:'post' do |f| %>

            <%= f.label 'Short Description' %>
            <%= f.text_area :description, class: 'form-control' %>
            <div>
            <br>
            <br>
            </div>
            <%= f.hidden_field :tournament_id, :value => @tournament.id %>


            <%= f.submit 'Add', class: 'btn btn-primary', id: 'add_player_btn'%>
            <% end %>
          </div>
        </div>
      </div>
      <div class="modal-footer">
        <!--empty now for spacing purposes-->
      </div>
    </div>

  </div>
</div>
